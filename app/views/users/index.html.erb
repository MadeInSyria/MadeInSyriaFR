<% provide(:title, 'Users list') %>
<div class="page-header">
  <h1>Users list</h1>
</div>

<%= will_paginate @users, renderer: BootstrapPagination::Rails %>

<ul class="users">
      <%= render @users %>
</ul>

<%= will_paginate @users, renderer: BootstrapPagination::Rails %>